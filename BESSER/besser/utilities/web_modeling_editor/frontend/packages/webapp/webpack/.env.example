# BESSER Frontend Configuration (.env.example)
# This file contains PUBLIC frontend configuration (no secrets).
# Copy to .env and update for your environment.
# 
# IMPORTANT: Backend secrets (GITHUB_CLIENT_SECRET, OPENAI_API_KEY, SMTP_PASSWORD, etc.)
# must NOT be in frontend .env — they belong in the backend .env only.
# Do NOT commit .env to git.
# 
# Backend configuration is in: .env (root)

# === Deployment URLs (Change for local/prod) ===
# LOCAL:
DEPLOYMENT_URL=http://localhost:8080
BACKEND_URL=http://localhost:9000/besser_api
UML_BOT_WS_URL=ws://localhost:8765

# PRODUCTION:
# DEPLOYMENT_URL=https://editor.besser-pearl.org
# BACKEND_URL=https://editor.besser-pearl.org/besser_api
# UML_BOT_WS_URL=wss://editor.besser-pearl.org/agent


# GitHub OAuth CLIENT ID ONLY (no secret here!)
# Secret is handled by backend at /besser_api/github/auth/callback
GITHUB_CLIENT_ID=your_github_client_id_here
