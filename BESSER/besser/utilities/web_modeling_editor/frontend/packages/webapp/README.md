# webapp (Legacy)

This package is **deprecated** and kept for reference only. Do not add new features or fix bugs here.

The active frontend is [`packages/webapp2`](../webapp2/), which uses Vite, Radix UI, and Tailwind CSS.
